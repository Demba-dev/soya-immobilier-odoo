<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!-- Vue du tableau de bord global amélioré -->
        <record id="view_soya_estate_dashboard_form" model="ir.ui.view">
            <field name="name">soya.estate.dashboard.form</field>
            <field name="model">board.board</field>
            <field name="arch" type="xml">
                <form>
                    <board style="1-1-1">
                        <column>
                            <action name="%(soya_estate.action_property)d" string="📋 Liste des Biens"/>
                            <action name="%(soya_estate.soya_visit_action)d" string="👁️ Visites"/>
                        </column>
                        <column>
                            <action name="%(soya_estate.soya_prospect_action)d" string="🎯 Prospects"/>
                            <action name="%(soya_estate.action_payment)d" string="💳 Paiements"/>
                        </column>
                        <column>
                            <action name="%(soya_estate.action_rental_contract)d" string="📜 Contrats Location"/>
                            <action name="%(soya_estate.action_sale_contract)d" string="📝 Contrats Vente"/>
                        </column>
                    </board>
                </form>
            </field>
        </record>

        <!-- Action pour accéder au tableau de bord -->
        <record id="action_soya_estate_dashboard" model="ir.actions.act_window">
            <field name="name">Tableau de Bord Immobilier</field>
            <field name="res_model">board.board</field>
            <field name="view_mode">form</field>
            <field name="view_id" ref="view_soya_estate_dashboard_form"/>
            <field name="target">current</field>
        </record>
    </data>
</odoo>
